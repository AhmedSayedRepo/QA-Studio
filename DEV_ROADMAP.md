# QA Studio — Dev Folder (roadmap & handoff)

**Folder:** `C:\Users\proga\Downloads\qa-studio` — the **live source of truth** for the app. Edits here get synced to the installed copy and released.

**Version:** `2.9.7` (see `VERSION`).

---

## How to read this file
Single source of truth for the dev app's state and backlog. A new chat can continue **without reading all of main.py** — use this file plus targeted reads of the specific method being changed. The big architectural refactor is happening in a separate folder (see "Modular refactor" below); do not re-derive it.

---

## What this app is
Flet **0.85.3** (pinned) desktop app — an Azure DevOps test-case / test-plan generator. Core files:

- `main.py` (~7,260 lines) — `class QAStudio`: all screens, shell/nav, window chrome, dialogs, self-update, login gate.
- `engine.py` (`E`) — providers/config (`E.AI_CONFIG`), generation, `apply_update`.
- `regression.py` — Regression + Test-Plan screens; `_checkbox_multiselect` (the custom inline dropdown used on Setup too).
- `sprint_titles.py`, `users_screen.py` — those screens.
- `theme.py` (`T`) — design tokens (`T.VIOLET`, `T.INK`, `T.R`, …) + `apply_theme`.
- `store.py` — DPAPI-encrypted creds (`store.load()` / `store.save(self.creds)`).
- `auth_supabase.py` (`auth`) — external auth gate + per-user permissions.
- `installer.py`, `install.bat`, `release.bat`, `_sync_to_install.py`.

### Toolchain / processes
- **Run (dev):** `C:\Users\proga\AppData\Local\Programs\Python\Python312\python.exe main.py` (GUI: `pythonw.exe`). This 3.12 has Flet; a co-installed 3.14 does NOT — desktop shortcuts must point at the 3.12 `pythonw`.
- **Sync to installed app** (`%LOCALAPPDATA%\QA Studio`): `python _sync_to_install.py` (uses Python shutil — does NOT truncate; never sync large files via shell `cp`, which has truncated `main.py` and broken launches before).
- **Release:** `release.bat` with a version bump.
- **Self-update:** `engine.apply_update` pulls the branch zipball (`AhmedSayedRepo/qa-studio`, branch `main`) and clears `__pycache__`.

---

## Session changes (2026-07-09)
- **Note on verifying fixes land:** the install at `%LOCALAPPDATA%\QA Studio` runs loose .py source (not the frozen exe from `build.bat`), so `_sync_to_install.py` correctly copies new code to disk, but an already-running QA Studio process keeps executing whatever it loaded at startup until it's fully closed and relaunched — syncing alone doesn't affect it. VERSION is bumped separately via `release.bat` when pushing to GitHub, not per dev change, so it isn't a reliable per-fix signal; the real check is simply: fully quit and relaunch after syncing before re-testing.
- **Automation — sequencing bug fixed (the real 'wrong sequencing' cause).** `validate_and_sequence_suite` (`engine.py`) sorted each story's cases with `key=lambda c: (c["bucket"], c["title"])` — the title was a SECONDARY sort key, so within a bucket cases were reordered ALPHABETICALLY by title text instead of keeping their authored order. That scrambles any intended step sequence (e.g. "create X" needs to run before "verify X exists"), and Arabic/English mixed titles sort by Unicode range, nowhere near logical order. Fixed to `key=lambda c: c["bucket"]` — Python's sort is stable, so cases now keep their original per-story order within each bucket. Applies to all three targets (Selenium/Playwright/Cypress) since they all consume the same `sequenced` output.
- **Automation — sequencing log, blank entries.** A case with no generated title used to log a blank line (silently skipped). Now logs `(untitled test case)` so every entry is identifiable.
- **Automation — Browse folder picker actually works now.** Root cause: `_browse_auto_folder` called its native-dialog attempts SYNCHRONOUSLY from the Flet `on_click` handler (Flet's own event-loop thread) — a blocking native dialog on that same thread silently failed every time (an earlier attempt at a zero-dependency ctypes/shell32 picker had the same problem and was reverted). Rewritten to mirror `regression._ask_save_path` exactly (the working exporters' 'Save As' dialog): a tkinter dialog run on a background thread via `threading.Thread`, tri-state return (`str` picked / `None` cancelled / `False` unavailable). Cancel now does nothing (no toast); the toast only fires when no native dialog is available at all.
- **Automation — README/.gitignore now always match the actual framework.** New `engine.sync_project_readme()` + `_detect_project_framework()`: on every `push_to_git()` call, detects the on-disk framework (`pom.xml`/`playwright.config.js`/`cypress.config.js`) and rewrites `README.md` + `.gitignore` from the correct template if they've drifted — covers a fresh generate-and-push AND pushing an old/forgotten-run folder via Browse that this session never regenerated. Also removed a second copy of the same bug: `push_to_git` had a hardcoded Maven-only `.gitignore` write that unconditionally overwrote whatever the sync had just written, on every push, for every framework (silently clobbering Playwright/Cypress `.gitignore`s).
- **Automation — Git push, friendlier rejection + Force-push retry.** A non-fast-forward push rejection used to dump git's raw multi-line hint text into the Activity log. `push_to_git` now returns one plain sentence (`"rejected: ..."`), and `_push_automation` (`main.py`) offers a confirm-dialog Force-push retry instead of leaving the user stuck.
- **Automation — auto-create the GitHub repo for a brand-new output folder.** New `engine._github_ensure_repo()` / `_parse_github_owner_repo()`: before pushing, if this folder has never been pushed before (no `git_pushed` marker in `.qastudio/manifest.json`), and the remote is `github.com`, check whether the repo exists and create it (private) if not, then push normally. Already-pushed folders skip the check. GitHub only — Azure DevOps/GitLab repo creation isn't wired up. Needs a PAT with repo-creation rights (classic `repo` scope, or fine-grained `Administration: Read and write`) — the PAT info icon (`main.py` HELP `"git_pat"`) and Help & guide → Automation were updated to explain this.
- **Automation — 'TODO' counter chip relabeled.** The Activity header chip literally read "TODO — locators resolved at runtime" (a leaked dev-facing label). Renamed to "Locators self-healed at runtime"; the counting logic is unchanged.
- **Auth — idle-logout dialog can no longer appear on the login screen.** `_show_idle_warning` and its countdown loop now bail/close quietly if the session isn't authenticated, instead of the countdown floating over the (unauthenticated) sign-in gate mid-session-end race.
- **Email reports — accent color now matches the app instead of an unrelated blue.** Both `build_report_email` (test-case generation run report) and `build_sprint_summary_email` had their own local `VIOLET="#3A57D6"` (a royal-blue, plus a deeper-blue `VIOLET_INK` and pale-blue `VIOLET_SOFT`) hardcoded just for email HTML — completely unrelated to the app's actual accent color (theme.py's `VIOLET`/`VIOLET_INK`/`VIOLET_SOFT`, which despite the name is a cyan/teal tone: `#0E9CC0`/`#0B6E86`/`#D6F4FB`). Both now use the app's real values, so the "Completed" status accent, headline count, CTA button, per-story section dot, and log "story"/"info" line colors in the emailed report all match the in-app color tone.
- **Run — added Copy log / Clear log, matching Automation.** The Run screen's RECENT ACTIVITY panel now has the same pinned Copy/Clear icon-button toolbar as Automation's Activity log (same visual "chip" style, Clear red-tinted). Refactored the clipboard-copy logic into one shared `_copy_log_text()` (Run and Automation both call it, so they can't drift out of sync) and added `_copy_run_log`/`_clear_run_log`. Clear is a scoped update to `_log_col` (no full `render()`), and correctly resets `_rendered_count` to 0 so a line logged right after Clear still renders (the Run screen's log uses an incremental "append only what's new since last render" optimization keyed off that counter — leaving it stale after a clear would have silently swallowed the next line or more until the log grew past the old count again).
- **Automation — Copy log's OS-clipboard fallback no longer hangs the next paste.** The tkinter-based fallback added for Copy log (`clipboard_clear()`/`clipboard_append()`) had a real, reproduced bug: Windows' clipboard normally uses delay-render — the text isn't actually handed to the OS at append time, the calling window just registers itself as able to produce it LATER, when something else pastes. That code destroyed the hidden Tk window immediately after, so the next paste (e.g. into Notepad) hung waiting for a response from an already-dead window. Replaced with `_win_clipboard_set()`, a direct ctypes `OpenClipboard`/`GlobalAlloc`/`SetClipboardData`/`CloseClipboard` write (same explicit-argtypes rigor as store.py's DPAPI code, to avoid 64-bit pointer truncation) — the text is committed to the system clipboard synchronously inside the call and ownership of the memory transfers to Windows itself, so there's nothing left alive for a later paste to wait on. Verified with a mocked-Windows-API test confirming the exact call sequence, correct UTF-16LE + NUL-terminator encoding, and that `CloseClipboard` still runs even if a later step fails.
- **Automation — Clear log is now a scoped update, not a full-screen render.** Matches the same reasoning as the push-flow fix: `_clear_auto_log` called `self.render()`, rebuilding the entire Automation screen (left-hand form included) just to empty the log. Now swaps `_auto_log_col`'s own content back to the empty-state placeholder in place, falling back to a full render only if the log column doesn't exist yet.
- **Setup — Azure Organization is now genuinely isolated per account.** `self.creds["org"]` was always stored per-account correctly, but three places fell back to the shared `E.AZURE_ORG` engine-global when the CURRENT account had none saved: the Setup screen's displayed field value, `_connect()`'s org resolution, and `_switch_user_creds()` never reset that global at all on an account switch. `E.AZURE_ORG` isn't itself per-account (it's a plain module global used for every live Azure DevOps API call) and starts out holding a hardcoded dev-default org — so a second account signing in on the same running app could see a PREVIOUS account's organization pre-filled in their own Setup field, silently save it into their own creds if they clicked Save/Connect without noticing, or even have live API calls run against the wrong org invisibly (field could look right while `E.AZURE_ORG` itself was still stale). Fixed: `_switch_user_creds()` now resets `E.AZURE_ORG` to this account's own saved value (or blank) on every switch; the Setup field and `_connect()` no longer fall back to `E.AZURE_ORG` — a genuinely unset org is now a required-field error on Connect, same as the PAT/API key, instead of a silent cross-account fallback. Verified with a scripted two-account test (Account A connects, Account B switches in — engine global and field both correctly go blank, Connect is blocked until B enters their own org).
- **Automation — fields (PAT, Git URL/branch, site/login URLs, project folder) now actually persist across restart/re-login.** Real root cause of "I pasted a new PAT and it wasn't there next time": `self.auto_git_token`/`auto_git_url`/`auto_git_branch`/`auto_site_url`/`auto_login_url`/`auto_login_user`/`auto_login_pass`/`auto_local_path` were only ever seeded ONCE, in `__init__` — which runs and reads `self.creds` **before** `store.set_user(uid)` ever points at the signed-in account's own per-user file (that switch happens later, asynchronously, once silent session-restore resolves who's signed in). So these fields were seeded from the SHARED DEFAULT file, not the account's own. The save itself was never broken — pasting a PAT correctly wrote it to the right per-user file once already signed in — but on the *next* launch `__init__` read the default file again before the real per-user file was ever loaded, so the field looked empty/reverted even though the save had succeeded. `_switch_user_creds()` (main.py) now re-syncs these 8 fields from the freshly-loaded per-user creds, the same way it already did for theme/lang/tool — applied unconditionally, since this only runs at sign-in / startup session-restore / sign-out, never while a field is actively being edited. Verified with a scripted test reproducing the exact before/after.
- **Auth — dark/light picked on the login screen no longer gets discarded on sign-in.** Real bug (found after the sign-out fix above still left the reverse direction broken): `login.py`'s sign-in handler used to apply + save `app._login_theme` (the login screen's own choice) to `app.creds` and call `store.save(app.creds)` **before** calling `app._switch_user_creds()`. But that save happened while the pre-sign-in (shared/default, `uid=None`) creds file was still active — `_switch_user_creds()` immediately after then loaded the signed-in account's own per-user file and, per its own theme-sync logic, overwrote `T.MODE`/`page.theme_mode` with **that** file's saved theme. Net effect: picking dark on the login screen and signing in landed you back in whatever theme that account last had saved (often light), silently. Fixed by reordering — `_switch_user_creds()` now runs first (loading the correct per-user file), and the login screen's chosen theme is applied and persisted into `self.creds` *after*, so it's the one that sticks.
- **Auth — login screen now matches the app's theme after sign-out.** Root cause (pre-existing, not introduced by the guard above): `login.py`'s login gate keeps its own independent theme flag, `app._login_theme` — it's only ever set by the login screen's own toggle or copied INTO `creds["theme"]` on a successful sign-in; nothing ever synced it back FROM the app's theme on the way *out*. So the first time an app instance returned to the login gate after a sign-out, `_login_theme` was still unset and fell back to its hardcoded dark default — a visible flip to dark even if the app/account that just signed out was in light mode. `_sign_out()` (`main.py`) now sets `self._login_theme = T.MODE` right after `_switch_user_creds()` re-applies the now-active creds file's theme, so the login screen opens in whatever theme was just on screen. First-launch behavior (login defaults to dark before anyone has ever signed in) is unchanged.
- **Investigated, NOT changed:** the Automation Activity log's Arabic-text line spacing was tweaked (1px → 4px padding) earlier this session as a guess, then rolled back to the original 1px on request — no reproducible evidence the padding was ever the actual cause (Flet's `Text` control has no `rtl`/direction property to fix bidirectional layout with either, confirmed against the current Flet docs). What looked like "overlapping text" in the case that prompted this was actually the sequencing bug above, not a rendering issue — leaving log-line padding as-is unless a concrete repro shows otherwise.
- **Automation — Git push no longer resets the Activity log scroll.** `_push_automation` used a full `self.render()` at push-start, on Force-push retry, and in `finally` — each rebuilding the whole screen (including a brand-new log Column), snapping scroll back to the bottom 2-3 times per push. The Generate/Push buttons and the ACTIVITY spinner are now persistent `ft.Container`s (`app._auto_buttons_ctr`, `app._auto_spinner_ctr`) refreshed in place via `automation._refresh_auto_state(app)`; the log column itself is never touched by a push, so scroll position holds.
- **Automation — Activity log entries no longer render out of order.** Root cause: `_auto_logmsg`'s UI update went through `ui_safe()`, which (via Flet's `page.run_thread`) starts a **brand-new thread per call** with no ordering guarantee — so under fast logging (e.g. sequencing 32 cases back-to-back) the on-screen `Column` could show "Sequencing case 1/32" before the "Sequencing N test case(s)…" header, or a case's title several lines away from its own "Sequencing case k/N" line, even though the underlying `self._auto_log` list was always correctly ordered. Fixed by (1) a `self._auto_log_ui_lock` serializing the column catch-up so concurrent `upd()` calls can't race on `len(col.controls)`, and (2) each `upd()` now appends every entry in `self._auto_log[have:real]` (read fresh from the authoritative list by index) instead of only its own closure-captured message — so whichever thread happens to run, first or last, always paints the full correct sequence. Verified with a scripted race test (multiple `upd()`s started in scrambled order, several runs) confirming on-screen order matches the log list every time.
- **Automation — Copy log actually copies now (and fails loudly, not silently, if it truly can't).** `_copy_auto_log` was reported failing with "Couldn't copy the log." every time on a long-running automation's log (dozens of `Sequencing case k/32` + Arabic-title lines, several thousand characters). Two bugs, both fixed: (1) the failure toast was rendered via `_toast` (always green/success-styled, per its own hardcoded `T.GREEN` + checkmark icon) instead of `_err` (red) — so a genuine failure LOOKED like a success in the screenshot that surfaced this. (2) The only clipboard path was `page.set_clipboard()`, which goes over Flet's own IPC channel to the Flutter client and may have an undocumented payload-size limit on a long log; there's now a fallback that sets the OS clipboard directly via `tkinter` (the same native layer already proven reliable for the Browse-folder picker) if `set_clipboard` raises, and only shows the red error toast if BOTH paths fail — and via a new `_clip_fail_reason()` classifier, that toast is now one plain-English sentence ("clipboard support isn't installed…", "no display is available…", "the app window didn't respond in time…", "another app has the clipboard locked…") instead of a raw exception dump, falling back to the exception's own (truncated) text only when nothing more specific matches.
- **Automation — Copy log / Clear log.** Added two icon buttons to the ACTIVITY header: copy the entire log to the clipboard (skips blank lines, toasts if there's nothing to copy), and clear it (`self._auto_log = []` + re-render). Later moved into a pinned toolbar row at the very top of the log rail itself (a fixed sibling of the scrollable log `Column`, inside the same bordered box, separated by a divider) so it stays put while the log scrolls beneath it, and redesigned as rounded icon "chips" — Clear is red-tinted to read as destructive, Copy stays neutral.
- **Automation — Stop no longer waits out the current AI call.** `validate_and_sequence_suite`'s per-case `compile_test_case(...)` call is a blocking HTTP request (up to a 90s timeout) that was invoked directly, so clicking Stop mid-case only took effect on the *next* case — up to 90s of apparent hang. Wrapped it in `_run_stopaware` (already used by the Run screen's Titles/Steps generation): a background thread runs the call while the main loop polls `should_stop()`/the global stop event every 0.3s and raises `StopRequested` the instant Stop is clicked, abandoning the in-flight request (harmless — it finishes on its own timeout with no side effects). Verified with a scripted test: Stop clicked ~0.3s into a simulated 5s blocking call aborted in ~0.3s, not 5s.
- **Setup — Email sender config is Admin-only.** The Gmail sender address/name + App Password fields are hidden from non-admin users in both the edit and saved views (`_connection_edit`/`_connection_saved`) — they're a shared, org-wide credential used to send reports for everyone, not a personal setting.
- **Setup — Email config now syncs org-wide via Supabase (configure once, applies to everyone).** New Supabase table `public.org_settings` (RLS: any authenticated user may SELECT, no direct write policy) + a new Edge Function `org-settings` (mirrors `admin-users`'s pattern: verifies the caller's JWT, GET is open to any signed-in user, POST upsert requires `app_metadata.role === "Admin"`, service-role key stays server-side). Client side (`auth_supabase.py`): `get_org_settings()` / `admin_set_org_email(sender, sender_name, app_password)`. `main.py`: saving the sender address, sender name, or Gmail App Password (`_save_sender`/`_save_sender_name`/`_save_gmail`) now also calls `_push_org_email()`, which — only if the caller is a signed-in Admin — pushes the trio to `org_settings` in the background; every sign-in/session-restore/sign-out (`_switch_user_creds`) calls `_refresh_org_settings()`, which pulls the shared value in the background and applies it via `E.set_credentials(...)`. Fully best-effort and non-blocking: auth not configured, not signed in, offline, function not deployed, or a non-Admin caller all fail silently or with a distinct non-blocking toast ("Saved locally, but couldn't share with other users: …") — the user's own local save (`store.save`) always succeeds regardless, so email sending is never blocked by the sync layer. Backend already deployed (no manual setup step needed, unlike `admin-users`).
- **Email reports — unified design across all three report types.** The run report, Sprint Summary, and Regression/Sprint Plan emails used three different palettes and layouts, including a leftover royal-blue/purple gradient (`#3A57D6`/`#6A33A8`) unrelated to the app's real cyan accent. All three now share one palette (theme.py's cyan `VIOLET`/`VIOLET_INK`/`VIOLET_SOFT` family) and the same masthead + hero-pill + metric-strip + section-head + footer chrome — `_plan_html` (regression.py) was rebuilt to match `build_report_email`'s design language; `build_sprint_summary_email` was already aligned. Each report keeps its own content-specific layout (per-story log vs. KPI/story-table vs. status breakdown) since the underlying data genuinely differs.
- **Titles — duplicate test cases still slipping through despite the anti-duplicate prompt.** A story with 14 existing test cases got 8 more that were largely near-duplicates with different phrasing (e.g. "requests submitted for the branch" vs "actions taken for the branch" — same assertion, different words). The existing rule-based filter (`_is_near_duplicate` + `_semantic_key`'s hand-curated `_AR_SYN` synonym table) only catches duplicates that share literal words or synonyms someone thought to add to that table ahead of time — it can never generalize to an arbitrary story's domain vocabulary. Added `_dedupe_titles_ai`: a second-pass, best-effort AI review that runs only on whatever survives the free filter (keeps the prompt/cost small), asking the provider — which already understands synonyms/paraphrase in any domain — to flag any remaining title that duplicates the *meaning* of an existing or earlier-listed one. Wired into both title-generation paths in `engine.py` (suites with existing cases, and freshly-seeded empty suites). Fails open: any AI/parsing error just returns the candidate list unchanged, so this refinement pass can never block generation. Verified with a scripted mock-AI harness (flagged-duplicate removal, no-duplicate pass-through, failure fallback, empty-input short-circuit).
- **Titles — the SAME synonym-blindness also affected cases already sitting in Azure, in both the Titles and Steps tools.** The previous fix only stopped NEW duplicate titles from being created; it did nothing for the 14 already-existing near-duplicates in the reported story, because the cleanup pass that runs on existing suites (`dedupe_existing_suite`) used the exact same rule-based-only matching. Added `_ai_duplicate_clusters`: after the cheap semantic-key grouping runs (and handles the obvious/exact cases for free), whatever's left standing alone gets one batched AI call asking which of those remaining titles are the same scenario in different words; any AI-confirmed group is merged in and goes through the existing keep/delete logic (keeps the case with the most steps, deletes the rest, logs `kept #<id>` / `removed old #<id> · kept #<id>` for every case). Since `dedupe_existing_suite` already runs at the start of BOTH `run_titles` and `run_steps`, this strengthens duplicate cleanup for the Titles tool and the Steps tool identically — no separate wiring needed for Steps. Added a `should_stop` param so clicking Stop skips the AI mop-up call outright instead of firing it needlessly. Verified with a scripted mock suite containing an exact duplicate (rule-based catches it) and a true-synonym duplicate (only the AI mop-up catches it), confirming the correct case is kept in each pair, IDs are logged, Stop skips the AI call, and an AI failure falls back to the rule-based-only result without breaking anything.
- **Titles/Steps — FOUND THE ACTUAL ROOT CAUSE of duplicates accumulating no matter what: every duplicate check was comparing blank titles.** `fetch_test_cases_for_suite` deliberately requests `witFields=System.Id` for a fast bulk-COUNT path, and — as `fetch_test_case_title`'s own pre-existing docstring already said — its `workItem.name` comes back blank because of that; the Run/Report screens already knew this and backfill titles per-case before displaying them. `dedupe_existing_suite` and the "does this suite already have cases" checks in `run_titles`/`run_steps` never did that backfill — they read `workItem.name` directly, always got `""`, and so every dedup check trivially found "no duplicates" and every already-populated suite looked empty and got seeded with a fresh batch on top of what it already had. This is why the two AI-dedup fixes above, however correct, never actually prevented anything in practice: they never had real titles to compare. Confirmed live — the app logged "Suite 103223 is empty" for a suite the Azure DevOps portal showed had 28 test cases. Added `fetch_existing_titles_for_suite(project, plan_id, suite_id)`: fetches the fast raw listing, then backfills any blank title via `fetch_test_case_title(id)` concurrently (same pattern already used by Run/Report), leaving already-populated titles untouched. Wired into `dedupe_existing_suite` (replacing its direct `fetch_test_cases_for_suite` + blank-title `_rec`) and into `run_titles`'s existing-titles check. `run_steps`'s empty-suite check needs presence only, not titles, so it now just checks `bool(fetch_test_cases_for_suite(...))` directly — faster and immune to the same bug by construction. Verified end-to-end with a harness that reproduces the exact real condition (raw listing returns blank names for every item): before the fix `dedupe_existing_suite` found 0 duplicate groups on data with 2 real duplicate pairs; after the fix it correctly finds and resolves both.
- **Titles/Steps — the very next live run of the fix above crashed: `Dedup skipped for suite 103223: frozenset({...})`.** With real titles finally flowing through, `dedupe_existing_suite`'s AI mop-up merge (added earlier this session) hit a case its pairwise merge logic didn't handle: the AI returning OVERLAPPING duplicate clusters for a tight run of near-identical titles — e.g. `[A,B]` and `[B,C]` instead of one `[A,B,C]`. The second group's target key had already been merged away by the first, so `groups[target_gk]` raised `KeyError(<frozenset>)`, and `str(KeyError)` is just `repr()` of its argument — hence the raw frozenset dump in the log. Replaced the pairwise merge with a proper union-find over the AI-returned groups: any chain of overlapping clusters now collapses into one correctly-merged group regardless of how the AI split them, and no key is ever touched twice. Verified with a harness that forces the AI mock to return exactly this overlapping/chained shape (`[1,2]`, `[2,3]`, `[3,4,5]` on 5 near-identical titles): before the fix, this crashed the whole dedup pass (leaving all 28-style duplicates untouched, silently swallowed by the caller's generic `except Exception`); after the fix, all 5 correctly collapse into one group with the most-complete case kept. Re-ran the full existing regression suite (blank-title backfill, exact + true-synonym dedup, should_stop, AI-failure fallback) against the fixed version — all still pass.
- **Diagnostic only — HTTP 400 on delete showed no detail.** `_classify_delete_error` had `if status_code: return f"HTTP {status_code}"` as its fallback path, which discarded Azure DevOps' own response body for any status code that wasn't 401/403/404/5xx — exactly the case for a 400 Bad Request, so the log could only ever say "HTTP 400" with nothing to diagnose from. Changed it to always append a truncated (220-char) snippet of the actual server response text to the classified/generic reason. Diagnostic only, not a fix: no delete behavior changed, it only surfaces what the server is actually saying so the real cause of the 400 can be identified. Verified in isolation against a fabricated 400 body; not yet exercised against the real failure.
- **Titles/Steps — detection finally worked (8 duplicate groups, 19 duplicates correctly identified), but every delete then failed with no reason given.** `delete_test_case` swallowed every failure into a bare `False` (both the SDK call and its raw-REST fallback were wrapped in blind `except Exception: pass`/`return False`), so the log could only ever say "delete FAILED" with nothing to act on. Changed it to return `(ok, reason)`: added `_classify_delete_error(status_code, raw)` which turns a 403/401/404/5xx or raw exception text into a short, plain reason (most likely here: "no permission to delete work items in this project — ask an Azure DevOps admin to grant 'Delete and restore work items'", since QA Studio's own Admin role is a separate, app-level concept from the underlying Azure DevOps PAT's actual permissions). Also stops retrying after the FIRST permission-classified failure instead of repeating the same guaranteed failure (and the same log line) for all 18 remaining duplicates — it still reports every duplicate found, just without wasting API calls proving the same thing 18 more times. Reworded the log for plain language per feedback that "duplicate group" wasn't self-explanatory: now e.g. *"3 test cases test the same thing — keeping #103733 (4 steps, the most complete)"* and *"could not remove — no permission to delete work items in this project · duplicate of #103733"*, plus a clearer run summary. Verified with a harness that forces every delete to fail with a permission error: confirms the real reason reaches the log (not "delete FAILED"), and confirms only ONE real delete attempt is made before the rest are skipped as permission-blocked.
- **`delete_test_case` switched to suite-removal-only (final approach, superseding the Test Management API attempt above).** The Test Management API delete (`_test_client.delete_test_case`) fixed the earlier HTTP 400 ("Use Test Management REST API to delete test artifacts") but a live run then hit a genuine `AccessDeniedException` — hard-deleting a work item needs the elevated "Delete and restore work items" permission, separate from normal suite-edit rights. Reverted to a previously-proven approach: only remove the test case from the SUITE (`_test_client.remove_test_cases_from_suite_url`), which needs just suite-edit permission and never attempts a hard delete. `dedupe_existing_suite`'s removed-victim log line was also restyled — amber (`warn`) tone + an explicit 🗑 icon instead of the green ✓ shared with newly-created tests, so a removal reads as visually distinct from a "success" creation.
- **Automation — duplicate test cases skipped before sequencing (in-memory, no Azure writes).** New `E.dedupe_case_list(cases, log, should_stop)`: same two-layer matching as `dedupe_existing_suite` (semantic-key grouping + AI mop-up), but skip-only — nothing is deleted from Azure, the most complete case of each duplicate set is simply the only one carried forward into `stories_payload` before `validate_and_sequence_suite` runs. Wired into `_start_automation`'s `work()`, per story. Each skip line now also logs `⏱ mm:ss` for how long that story's dedup check took.
- **Report screen — Copy / Clear log buttons**, matching Run and Automation (`report.py`), reusing the same shared `_copy_log_text()`/`_copy_run_log`/`_clear_run_log`.
- **New searchable multiselect + chips email-recipient picker** (`regression.email_recipient_picker`) — sourced from the Azure DevOps project's member directory (`E.fetch_project_members`, Core API teams→members, cached per project) plus free-typed custom emails (regex-validated). Replaces the old plain-text email `TextField` on **Setup**, **Sprint Plan**, and **Regression Plan**; each member chip's tooltip shows their email. Same `_checkbox_multiselect` component the stories/plans pickers already use, so search/select-all/in-place sync all come for free.
- **Sprint Plan — Resources field uses the same member picker** (`regression.resource_name_picker`), keyed by NAME instead of email, for "Assign to Tester" — avatars use the matched member's initial/color, tooltip shows their email (or "not in the member list — typed manually" for a custom name). Layout kept as **Count + Resources side-by-side** (label "Resources", not "Testers"), with chips nested under the Resources field only — restored after an interim layout regression.
- **Automation screen — accessible as soon as you're connected + have a project** (Phase 10, the big one). Previously gated on Setup already having a project + plan + stories selected; now matches Regression/Sprint Plan's gate. New pieces, all added without touching Regression Plan's own working code:
  - `regression._auto_init(app)` / `regression._reload_auto_plan_stories(app)` / `regression.automation_source_card(app)` — a twin of Regression Plan's "Source & stories" Card 1 (multi-plan + story checkbox-multiselect, chips, lazy `app._plans` loading), writing to its own `_auto_`-prefixed state so it's fully independent of Setup's `plan_id`/`story_ids`. Inserted as card **A** in `automation.py`; the four existing cards shifted to **B–E**.
  - **Generate automation scripts** is disabled until at least one plan and one story are picked in the new section (`ready = bool(app._auto_plans_selected and app._auto_selected)`).
  - New card **F**, "Report email (optional)" — the same `email_recipient_picker`, writing to `app._auto_email_to`.
  - `_start_automation`'s `work()` adapted for **multi-plan**: stories now carry their own `plan_id` (like Regression Plan's selection), so suite discovery loops `E.discover_suites_for_stories` per distinct plan and merges the suite maps, instead of assuming one shared `self.plan_id`.
  - New `E.build_automation_report_email(...)` — same masthead/hero/metric-strip/log/footer chrome as `build_report_email` (one consistent brand across all report emails), with a Stories/Test cases/Skipped/Self-healed/Time metric strip and a Destination section (output folder + Git repo). Sent via `E.send_report` at the end of `work()` (success, "nothing to generate", or failure — not on the early-stop/cancel paths), gated on `app._auto_email_to` being set and Gmail being configured.
  - A minor found-on-the-way bug: `automation_source_card` referenced the shared `_reg_plans_loading`/`_reg_plans_for` state without guaranteeing it existed — if Automation was the FIRST screen opened this session (never having visited Regression Plan), this raised `AttributeError`. Fixed by also calling `regression._init(app)` (idempotent, `hasattr`-guarded) inside `automation_source_card`.
- **Automation — the "idle-looking gap" between "Connecting to Azure DevOps…" and the first real progress line.** Fetching every selected story's suite test cases + every case's steps (Phases A/B in `work()`) makes no `cb()` calls while it runs, so a large selection could look stuck for a while. Added two intermediate log lines: "Fetching test cases and steps for N stories…" right after connecting, and "Checking for duplicate test cases…" right before the per-story dedup loop starts.
- **Automation — per-story tracking during multi-story runs.** Neither the dedup-skip step nor `validate_and_sequence_suite`'s "Sequencing case k/N" line said WHICH story was currently being processed. Both now log a `▸ Story N → suite M · Title` line (same format/tone as the Run log's story lines) immediately before that story's work starts.
- **Automation — mixed English/Arabic log lines were rendering wrong.** `_auto_log_line`'s RTL detection triggered on ANY Arabic character anywhere in the message, so English-scaffolded lines with an embedded Arabic title (e.g. `"skipping duplicate: <Arabic title> (same as #103921)"`) got forced into full right-aligned RTL, garbling the English prefix/suffix around the title. Now only triggers RTL when the line *starts* in Arabic (a raw title), so scaffolded lines stay left-aligned/readable.
- **Run — same root cause, in the run email report.** `run_steps`' story→suite log line set `"ar": bool(title)` — true for ANY non-blank title, English included — so every "Story 101048 → suite 103220 · Change Language"-style line in the run activity log (and its email) was forced right-aligned even for English titles. Fixed to check whether the title is *actually* Arabic. Also: `run_steps`' timer (`_run_start`) used to start AFTER the pre-existing-duplicate removal + empty-suite-seeding passes, so that real elapsed time was silently excluded from the run's reported "Time" stat — moved to the top of the function, before suite discovery.
- **Automation — Time + Skipped stats.** New live "Duplicate cases skipped" counter chip next to the existing "Locators self-healed at runtime" one in the Activity panel (updated via a `SKIPPED_LIVE: N` hidden control line, mirroring the existing `TODO_LIVE: N` pattern for the self-healed counter), and the automation report email's metric strip now includes **Skipped** and **Time** (`E._fmt_secs`, timed from `self._auto_run_start`, set when Generate is clicked) alongside Stories/Test cases/Self-healed.
- **Nav rail — logo aligned with "QA Studio" text.** The brand `Row` (logo + title/version block) had no explicit `vertical_alignment`, so the 38px logo centered against the whole two-line block (title + version/Check-updates row) rather than the title line specifically. Now `CrossAxisAlignment.START`, top-aligning the logo with "QA Studio".
- **App icon — rebuilt with real transparency, then swapped to the actual brand asset.** `app.ico` previously had no alpha channel at all (fully opaque, `app.png`'s full-bleed square baked in) — first rebuilt as a proper multi-size (16–256px) icon with real transparent corners from the existing circular avatar asset, then replaced again with the actual source glyph (`qa-logo.png` — transparent, correct blue→purple gradient) once provided. `app.png` (the fallback every in-app logo/favicon reference resolves to first — nav rail, login/modal screens, installer favicon) now matches it exactly, so the icon is consistent everywhere: Desktop shortcut/taskbar/window titlebar (`app.ico`), in-app branding (`app.png` via `ui.py`), and report emails (already preferred `qa-logo.png` directly). Old versions kept as `app.ico.bak`/`app.ico.bak2`/`app.ico.bak3`/`app.ico.bak4`/`app.png.bak`.
- **Security audit + fixes (5 findings, all closed, independently retested twice).** A full pass over auth/credential handling, the self-updater, git push, and local storage:
  - **H-1 (High) — shared Gmail App Password was fetchable by ANY signed-in user, including zero-capability self-registered Viewers.** `main._refresh_org_settings()` fetched `auth.get_org_settings()` unconditionally on every sign-in and loaded the org-wide App Password into `engine.GMAIL_APP_PASS` regardless of role — a Viewer's own process (fully user-controlled, desktop app) could read it via a debugger. Client fixed: gated behind `auth.can(self.user, "act.export")`. Server fixed too (the real boundary): `supabase/functions/org-settings/index.ts` previously had NO capability check on GET at all (any valid token → full read) — added `hasExportCap()`, mirroring `auth_supabase.py`'s `ROLE_PRESETS` (Admin/Member have `act.export`, Viewer doesn't), and **redeployed live** to the `QA Studio` Supabase project (`psiyktcrggmgralyswua`) via the Supabase MCP — version 2 is active now, not just committed source (Edge Functions don't auto-deploy from a repo push).
  - **M-1 (Medium) — admin-bootstrap SQL in `ADMIN_USERS_SETUP.md` had no `WHERE` clause**, so running it as documented would grant Admin to every row in `auth.users`, not just the operator's own account. Added `where email = '...'` + an explicit warning + a verify-first `select`.
  - **M-2 (Medium) — self-updater installed an unverified binary if the GitHub release published no checksum.** `_verify_download` (`engine.py`) now fails CLOSED (rejects the update) instead of warning-and-proceeding — this is the path that downloads and re-launches an executable, so an unverified download was a real code-exec risk if a release's checksum file was ever omitted (attacker or mistake).
  - **L-1 (Low) — Git PAT sat in `.git/config` in plaintext during `push_to_git`** (`git remote add origin https://<token>@...`), cleaned up by a post-push `set-url` that would never run if the process crashed mid-push. Fixed twice: first to a `-c http.extraheader=...` argument (no more disk persistence), then — after the retest flagged the base64 token still being visible in the process's own argv/cmdline to other local processes — switched again to git's native `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_0`/`GIT_CONFIG_VALUE_0` env-var mechanism, so the token never touches disk OR argv.
  - **L-2 (Low) — non-Windows credential fallback was plain `base64` (no real encryption)** in `store.py` when DPAPI is unavailable. Added a Fernet (AES + HMAC via the already-pinned `cryptography` package) fallback keyed by a separate 0600 key file; base64 is now last-resort only if `cryptography` itself is missing. Retest also caught a corrupted-key-file edge case (would have silently downgraded to base64 with no warning) — `_fernet_key()` now validates the loaded key and regenerates on failure instead of falling through.
  - Verification: ran the `security-advisor` plugin agent (Claude Fable 5) as an independent retest after the first round of fixes, which correctly found H-1 only partially closed (client-side only) plus the two new argv/key-corruption issues above; ran it a second time after those follow-ups — all five original findings plus both retest-discovered issues confirmed Resolved.
- **Automation — counter chips no longer clip their own labels.** `_auto_counts_header`'s "Locators self-healed at runtime" / "Duplicate cases skipped" chips crammed a dot + long label + count onto one unbounded `Row` inside a ~185px pill, so the label text got cut off mid-word by the pill's own rounded corners once it ran out of room (`Row` children don't wrap). Restacked as dot+count on one line with the label in its own `Text` below (wraps up to 2 lines, ellipsis as a last-resort safety net) — width is now bounded by the parent `Column`, so it actually wraps instead of clipping.
- **Branding — new blurred-glow icon treatment, tuned twice.** Extracted a clean high-res crop of the glyph from `qa-logo-full.png` and built two variants from it: (1) a soft blurred blue→violet glow baked into the image itself, replacing the flat white `bgcolor="#FFFFFF"` card previously drawn behind the logo in `login.py` (both header and hero placements) and the drop-shadow used in the nav rail (`main.py`) — after a first pass the glow was toned down (lower blob opacity, smaller radius, slightly less blur) per feedback; and (2) `qa-logo-email.png`, a plain NO-glow crop at a larger size, used only in report emails (`engine._logo_path`/`_logo_tag`, now defaulting to 40/24px instead of 34/20px) since the glow suits an in-app dark surface but not a plain email background. Login header/hero logos and the nav-rail logo were also sized up a bit (40→48px, 48→60px, 38→48px respectively) as part of the same pass.
- **Email pickers — send button now sits beside the recipient picker, not stacked below, everywhere.** `regression.email_recipient_picker()` gained a `trailing=` param that places a control (the Send/Email button) next to the picker's collapsed trigger field in a `Row`, instead of the caller stacking the button on its own line underneath; wired into **Sprint Plan** and **Regression Plan**'s email sections. The **Sprint Summary** modal (`modals.open_sprint_summary`) — which already had this side-by-side layout, but with a bare comma-separated `TextField` instead of the real picker — was switched to the same `email_recipient_picker` component (own state key, `_sum_email_to`, seeded from but independent of the Report screen's `app.emails` so picking recipients here doesn't silently change the Report screen's queue) for a consistent picker+button pairing across all three "email this" surfaces.
- **Sprint Summary modal — status-breakdown cards were hardcoded to light-theme pastel hex.** `_PALETTE` mixed a theme-aware foreground (`T.VIOLET_INK` etc.) with a flat, LIGHT-ONLY background hex per status (e.g. `"#ECE8FF"`), so in dark mode the "25 / Product Owner Review", "4 / Hold", etc. cards rendered as bright pastel boxes clashing with the dark surface around them. Backgrounds are now derived from the foreground color via `ft.Colors.with_opacity(0.14, fg)` — a translucent tint over whatever's actually behind it — the same technique the card's own border already used one line below, so it's correct in both themes without a second hardcoded table.
- **Sprint Summary modal — the story link is now just the `#id`, not the entire row.** Each story row used to be one big `on_click` target (with an ink ripple) opening the work item in Azure DevOps, so selecting a title's text or just reading a row could accidentally navigate away. The `#id` is now its own small pill (violet text + an open-in-new icon) with the `on_click`/tooltip/ripple moved onto just that; the row itself is inert.
- **Dialogs — click-away-to-close now works for dropdowns opened INSIDE a modal, not just on the main screen.** `_close_dropdowns` (triggered by an empty-space tap) only ever fired from a `GestureDetector` wrapping the main screen `body` (`shell()`) — `AlertDialog`s render on a separate overlay layer outside that tree, so a picker opened inside a dialog (e.g. Sprint Summary's new email picker, once added above) never got a click-away trigger even though the exact same component works correctly when embedded directly in a screen (Sprint Plan, Regression Plan). Fixed centrally in `dialogs.show_dialog()` — every dialog's content is now ALSO wrapped in `SelectionArea(GestureDetector(content=c, on_tap=app._close_dropdowns))`, the same "child `GestureDetector` wins the empty-space tap over the `SelectionArea` ancestor, while checkboxes/buttons keep their own" trick already used for the main body — so this is fixed for every current and future modal in one place, not per-dialog.

## Session changes (2026-07-05)
- **Providers — Manus info modal** now points at the Manus API-keys page (was falling back to the Anthropic console because `PROVIDER_KEY_HELP` had no `manus` entry).
- **Providers — free tier added:** `groq`, `cerebras`, `openrouter`, `mistral` — all OpenAI-compatible, so they share `_openai_compat_http` / `_openai_compat_models_http` via the new `OPENAI_COMPAT_PROVIDERS` tuple (`engine.py`). Wired into `AI_CONFIG`, `STATIC_MODELS`, `T_disp`/theme `PROVIDER_DISPLAY`, `PROVIDER_KEY_HELP`, `PROVIDER_BRAND`. The provider dropdown is **grouped free/paid** (`FREE_PROVIDERS` set on `QAStudio`), with styled non-selectable group headers and `menu_height` scroll; `_on_provider_change` ignores `__grp_*` header keys.
- **Providers — NVIDIA per-model keys.** Only providers in `PER_MODEL_KEY_PROVIDERS` (currently `{"nvidia"}`) store their key per model, via `_cred_slot(name)` → `nvidia::<model>`. `_migrate_key_slots()` (run after every `store.load()`) moves a legacy bare-name NVIDIA key onto the current model. All key reads/writes go through `_cred_slot`.
- **Providers — model discovery** now returns the FULL catalogue: Gemini paginates (`pageSize=1000` + `nextPageToken`), Anthropic paginates (`has_more`/`last_id`). `_is_chat_model_id` relaxed to keep vision/VLM chat models (drops only precise image-GEN markers: `diffusion`/`flux`/`sdxl`/…).
- **Connect** for OpenAI-compatible providers now validates with a **cheap GET /models** instead of a chat "ping" — large models (NVIDIA 400B MoE) no longer time out Connect. NVIDIA default model → `meta/llama-3.1-70b-instruct`.
- **Generation JSON, the big one.** JSON mode forces an OBJECT on OpenAI-compatible providers, but the title/step prompts asked for an ARRAY → a model wrapped it under an invented key (`{"حالات الاختبار":[...]}`), and iterating the dict yielded that key as a single bogus title. Fixed: prompts now request `{"titles":[...]}` / `{"steps":[...]}`; `_coerce_title_list` / `_coerce_step_list` flatten any shape; `parse_json_robust` hardened (`strict=False` for literal newlines + `_balanced_json_fragment` to recover truncated JSON); compile/`generate_titles`/`generate_steps` `max_tokens` bumped to 4096. Same class of bug fixed in the intent compiler (`compile_test_case` → `{"intents":[...]}` + unwrap). The intent prompt was also restructured (ROLE / INPUT / OUTPUT schema / RULES / EXAMPLE).
- **Runs — Stop + heartbeat.** `_run_stopaware(fn, should_stop, on_slow)` runs a blocking AI call in a daemon thread and unwinds on Stop at once (the abandoned request dies on its own timeout; generation has no side effects). `generate_titles`/`generate_steps` are stop-aware with interruptible backoffs and a ~15s "still generating…" heartbeat.
- **Runs — Run screen.** Smooth per-second elapsed via `_start_meta_ticker` + `_tick_run_meta`; `_run_meta_line` derives a fallback pct from stories-completed so **multi-story titles** get a counting-down ETA (single-story titles inherently can't). `classify_ai_error`/`_is_credit_error` broadened + HTTP **402 → credit** so a truly-exhausted account STOPS like Anthropic; plain rate-limits still retry.
- **Automation — runtime healer generalized.** `AnthropicClient` → `AiClient` (speaks Anthropic / Gemini / any OpenAI-compatible endpoint). `Config.java` bakes `AI_PROVIDER`/`AI_BASE_URL`/`MODEL` from the generating connection (`_healer_ai_meta()`) and reads the key from `QA_AI_API_KEY` (falls back to provider-specific vars). The "Done" hint + README name the actual provider.
- **Automation — dead code removed** (~600 lines): the legacy LLM-writes-Java path (`generate_test_class`, `build_automation_project`, `build_or_merge_project`, `generate_additional_methods`, `_split_generated_code`, `_pom_xml`, `_base_test`, `_base_page`, `_testng_xml`, `_splice_members`) and `scrape_dom`. The live-walk `explore_and_map` is ALSO unused but was left in place (a 620-line block can't be safely deleted via the edit tool without a local compiler); its helpers `_harvest_dom`/`_harvest_errors`/`_verify_logged_in` were restored so it stays internally consistent. **TODO: remove `explore_and_map` + `_to_locator` + `_match_step_to_element` locally (with `py_compile`).**
- **Auth / security — idle auto-logout.** `_start_idle_watch` signs the user out after `_idle_minutes()` idle (Settings → Security card, **Admins only**: Off/5/15/30/60). Final 60s shows a "Are you still there?" countdown dialog (`_show_idle_warning`) with Stay signed in / Sign out now. A PAUSED automation counts as idle (doesn't block logout).
- **Auth — provider persistence + per-user isolation.** `_provider_choice` is saved to `creds["provider"]` and restored on launch (always resolves to a REAL provider, never `None` — that None fed `T.disp_name` and crashed Setup). `_switch_user_creds` now also clears `_log_lines`/`_auto_log`/run+automation flags/`last_report` so a new user never sees the previous session's activity.
- **UI — Help & guide.** New `help_guide.py`: a searchable feature guide (left nav of features + briefing pane), opened from a nav-rail "Help & guide" button (`_open_help_guide`; supports `_open_help_guide("automation")` for deep-links). Content lives in `help_guide.FEATURES`.
- **UI — nav rail.** Brand (logo + a visible, hover-animated **Check updates** pill via `_check_updates_chip`) and the PIPELINE label are PINNED; only the nav list scrolls (`_rail_nav_column`, plain scroll — offset is NOT preserved across re-render, accepted). Help/Settings/theme buttons got the nav hover (violet tint + slide) via `_rail_btn_hover`.
- **UI — modals.** `dialogs.show_dialog` auto-wraps multiple `dlg.actions` into ONE right-aligned Row, so **every** modal's buttons sit side-by-side (Flet otherwise stacks them). Manually-wrapped dialogs (single-Row actions) are skipped.
- **Note on the sandbox:** during this session the assistant's mount served TRUNCATED / null-byte copies of the big files (`engine.py`, `main.py`), so a live `py_compile` of the whole files wasn't possible there — edits were verified by function-extraction + host reads. **Run `py -3.12 -m compileall -q .` before release.**

## Recent changes already in this folder
- **"What to generate" default toggle** in Settings (Appearance card): choose Titles vs Steps as the default generator. Persists via `self.creds["tool"]` (`_set_tool` saves; loaded at init); `_tool_segment(compact=True)` renders it.
- **Dropdown click-away fix** (`shell()`): body wrapped in `SelectionArea(GestureDetector(content=body, on_tap=self._close_dropdowns))` so clicking empty space closes open dropdowns. The `SelectionArea` (added earlier for copyable text) was intercepting the taps; a child `GestureDetector` wins the empty-space tap while checkboxes/buttons keep theirs and drag-to-select still works.
- **Close-during-run confirm** (`_with_window_chrome` → `_close`): now shows `_confirm_close()` ("stop after current test case?") when `_run_active`/`_auto_running`, else `_force_close()`. The frameless window has no native X, so the custom close button is the only close path and previously force-closed mid-run without asking (`_confirm_close` was unwired).

---

## Post-merge changes (2026-07-03)
- **Viewer / read-only = view-only** — `shell()` sets `body.disabled = self.readonly`, which propagates to every button/dropdown/field/toggle in the content (nav + header stay live so they can still browse + sign out; scrolling unaffected). Read-only is already true for Viewers on every action screen (they lack the `act.*` cap).
- **Viewer content-view** — read-only users now **skip the "connect first" gate and see the real screen content, disabled** (empty states, no data — no creds/stories used). Each gated screen's condition became `if not app.readonly and not (connected...)` (Automation, Regression, Sprint Plan, Sprint Report). **Loader loop fixed properly (PERF-plan step 1):** the 3 build-time auto-loaders (`_cp_load_iterations`, sprint-report `_load_iterations`, regression plans loader) used a *falsy* "loaded" guard (`if app._cp_iterations` / `if not app._plans`), so an empty/sparse result reloaded + full-rendered forever — hit **Members too**, not just Viewers. Now they use a **key-based guard** (`_cp_iter_for`/`_st_iter_for`/`_reg_plans_for == app.project`): empty counts as loaded, reload only on project change, stale-drop on mid-load project change. Plus `connected`-guard (Viewers never fetch) and key/cache reset in `_switch_user_creds`. Story loaders are event-triggered, not build — unaffected. See `PERF_ARCH_PLAN.md`. Also: screens with **no action cap** (Useful Links) are now read-only for Viewers — `render()` sets `readonly = user has no act.* caps` when `_screen_action_cap` is None, so `shell()`'s `body.disabled` kicks in. (The earlier view-only placeholder was reverted.)
- **Per-user credentials + links** — `store.set_user(uid)` points load/save at `creds_<uid>.dat`; `main._switch_user_creds()` (called on sign-in / session-restore / sign-out) reloads that user's creds, resets connection, and re-applies their theme/lang/tool. `_links_path()` is now `links_<uid>.json` and the `_links` cache is dropped on user switch. Signed-out → shared default file.
- **Links open in front** — `_open_url` uses `os.startfile` on Windows (ShellExecute foregrounds the browser over the app); Useful Links routes through `_open_url`.
- **Landing page** — redesigned to match the app (dark neon/glass, v2.8.1) in `landing.html` — replace the GitHub Pages `index.html` with it.
- **Repo private?** — NOT safe as-is: install.bat, self-update, version-check, and GitHub Pages all rely on public unauthenticated access. Go private only after moving distribution (installer + VERSION + release zips + Pages) to a small PUBLIC repo, keeping source private. Audit for secrets first (anon key OK public, service-role NOT).
- **Enter submits login** — `login.py` wires `on_submit = _submit` on the email/password/name fields, so Enter triggers Sign in / Sign up.

- **Nav badges** → unique mnemonics across ALL tabs: Setup=S, Run=Ru, Report=Rp, Regression=Rg, Sprint Plan=SP, Sprint Report=SR, Automation=A, Useful Links=L, Users=U (pipeline in `theme.py NAV`; aux in `main.py __init__` NAV.insert). No collisions.
- **Existing-steps modal** (`modals.py`) — Cancel/Continue wrapped in one Row so Flet's action bar stops stacking them.
- **Users screen** (`users_screen.py`) — taller list container; each row has a red **Revoke access** action (confirm dialog). Soft revoke = strip all caps via `auth.admin_revoke_access` (new; uses the existing `admin-users` Edge Function, reversible by setting a role). No hard delete. Revoked rows show an **ACCESS REVOKED** pill, highlight no role, and disable the revoke button. **Revocation now takes effect within ~25s on the signed-in user** (was: only on next re-login — the JWT caches `app_metadata`): `render()` throttle-calls `auth.revalidate()` (GET `/auth/v1/user` → live DB `app_metadata`, not the stale JWT payload), updates `self.user`, and re-gates. A zero-cap user gets a locked **"Access revoked"** screen (`_no_access_screen`) with only Sign-out — no usable Setup/Settings/dropdowns/updates. `_first_allowed_screen` returns `None` when nothing is permitted (was: fell back to `setup`).
- **Providers refactor — COMPLETE + CLEANED** (`engine.py`) — pure-HTTP adapters (`requests`) for ALL providers (openai/nvidia/deepseek/qwen, anthropic, azure, gemini, manus; ollama already HTTP), across generate (`_ai_call_once`), connect (`validate_api_key`), and model-list (`list_models`). **Flag removed** — HTTP is the only path; all SDK branches deleted; no vendor SDK imported. `requirements.txt` trimmed by 16 pkgs (anthropic/openai/google-generativeai + grpcio/protobuf/google-* tree) — kills the Python/dep-mismatch install failures at the root. See `PROVIDERS_REFACTOR_PLAN.md`.

## UI polish (2026-07-03)
- **Searchable multiselect story pickers** — `regression._checkbox_multiselect` gained a type-to-filter search box (shown when >6 options) + a `disabled` flag (greys + blocks the field). Used on **Setup, Regression, and Sprint** screens, so all three get search for free (consistency). Search hides/shows the lazily-built row containers in place (`row_conts`), with a "No matches." hint.
- **Setup story section reworked** — removed the manual "paste IDs" input (`self.story_field` kept alive but unrendered so its legacy handlers stay valid) and the single-add searchable dropdown. The story picker is now the searchable **multiselect**, **always displayed** and **disabled until a test plan is picked** (placeholder reflects state: "Select a test plan first" → "Loading stories…" → "Select stories"). Chips still show/removed via the existing `_dd_syncers["setup_stories"]` sync. `_load_setup_stories_inplace` now re-renders once on plan-change completion (single event; scroll snaps back) instead of patching the removed dropdown.
- **Header step badge = nav badge** — `shell()` auto-derives the header pill from the active nav's `ix` (S/Ru/Rp/Rg/SP/SR/A/L/U) when no explicit badge is passed; removed the two hardcoded ones (`STEP T`, `U`).
- **Hover-border affordance — rolled out app-wide** (`ui.py`) — `hover_border_cb`/`hover_field` helpers tint a field's border violet on hover (Flet's TextField/Dropdown have no native hover-border). `hover_field(field)` wraps a field in a `Container` that mirrors its `expand` flag and applies the hover tint, unless the field is `read_only`/`disabled` (returned unwrapped). Coverage, beyond the initial Setup connection + task-card fields:
  - **Create Plan modal & Sprint Summary email modal** (`modals.py`) — `name_field`, `iter_dd`, `email_field`.
  - **Stories multiselect** (`regression._checkbox_multiselect`) — no TextField/Dropdown backs the closed trigger (it's a plain `Container`), so it gets a bespoke `_field_hover` that mirrors the same violet-on-hover logic directly on `field_container.border`, skipped while the panel is open (already violet) or `disabled`. The in-panel search box (`search_tf`) uses `hover_field` normally.
  - **Regression/Sprint screens** (`regression.py`) — resource count/name fields, per-story hours/assignee row fields (memoized, so the wrap is one-time per row), plan/sprint email fields, the `_num()` min/max estimate factory.
  - **Automation screen** — `main._auto_field` (the shared per-field factory every Automation input goes through) now wraps its `TextField` in `hover_field`, so all Automation fields get it for free.
  - **Command palette search field** (`main.py`) and **Users screen search** (`users_screen.py`) and **Useful Links add-link fields** (`useful_links.py`).
  - **Login screen intentionally excluded** — `login.py` uses its own glass/photo-background design system (`FIELD_BG`/`FIELD_BD`/a per-theme `accent` color, not `T.BORDER`/`T.VIOLET`), so the generic hover-to-violet tint would clash; it needs its own accent-aware hover helper if wanted later.
  - Found and fixed a **real pre-existing corruption bug** in `regression.py` while wiring this up: the file's Sprint-screen `screen()` function was truncated mid-statement (hard cut inside `ft.Container(card3, ex` — the exact issue this file already flagged below as unverified). Restored the missing tail from git HEAD (that specific tail region was unmodified by any in-flight feature work, confirmed via diff) and re-verified structurally: `regression.py`'s top-level `def`/`class` list now matches HEAD's 60 entries 1:1, and `badge="STEP R")` appears exactly once (no residual duplication). **Note:** this session's sandbox could not get a live `python -m py_compile` against this connected folder (its mount only reflected a stale pre-session snapshot throughout, despite edits succeeding on the real files) — verification here was via direct file reads + structural diffing instead of a compiler. Recommend running `python -m py_compile *.py` and clicking through every screen before the next release, same as the existing "Next" step below.

## Modular refactor — MERGED ✅ (2026-07-03)
The refactor is merged into this folder: `main.py` is now **4,334 lines** and imports the 12 modules (`ui`, `useful_links`, `settings`, `run`, `report`, `automation`, `setup`, `dialogs`, `updater_ui`, `window_chrome`, `login`, `modals`), all passing the AST gate here. Dead files removed: `auth.py` (unused), `qa_perf.log`, stale `__pycache__` (pyc for deleted `web_login`/`auth_screen`/`login_assets`). The 3 in-flight fixes (click-away, confirm-close, what-to-generate) are present post-merge.

**Next:** launch on 3.12 and click every screen; then `python _sync_to_install.py`, bump `VERSION`, `release.bat`.

Superseded planning docs removed: `REFACTOR_PLAN.md`, `AUTH_PLAN.md`, `AUTH_EXTERNAL_PLAN.md`. Remaining docs: `README.md`, `DEV_ROADMAP.md` (this file), `ADMIN_USERS_SETUP.md`.

## Modular refactor (historical notes)
`main.py` is being split into per-screen / per-concern modules in **`C:\Users\proga\Downloads\qa-studio-refactor`** (a standalone copy). Done there: `ui.py`, `useful_links.py`, `settings.py`, `run.py`, `report.py`, `automation.py`, `setup.py`, `dialogs.py`, `updater_ui.py`, `window_chrome.py`, `login.py`, `modals.py` (12 modules) — `main.py` shrunk ~7,250 → ~4,334 lines with **no intended behavior change**. All screens, cross-cutting concerns, and the dialog/modal builders are extracted; the dead WebView2 cluster is removed (117 lines). What remains in `main.py` is the app shell + state + orchestration + interdependent runtime glue (kept intentionally). The refactor is **complete**; next is the merge-back. See that folder's `REFACTOR_ROADMAP.md`.

**Merge plan (when validated):**
1. Copy the new module files + refactored `main.py` from the refactor folder into this folder.
2. Run the AST gate (in `REFACTOR_ROADMAP.md`) here; then launch on 3.12 and click every screen.
3. `python _sync_to_install.py`, bump `VERSION`, `release.bat`.

Until merged, apply small fixes to **both** folders (like the click-away fix) or note the drift.

---

## Backlog / open items
- Merge the modular refactor back (above) once the refactor folder is fully validated.
- **Remove the dead live-walk explorer** — `explore_and_map` + `_to_locator` + `_match_step_to_element` (and their exclusive helpers `_harvest_dom`/`_harvest_errors`/`_verify_logged_in`) in `engine.py` are unused (the active path is `generate_and_push_selfhealing`). Left in this session because a 620-line block delete wasn't safe without a live compiler; do it locally with `python -m py_compile engine.py` as the gate.
- ~~Remove leftover WebView2 dead code~~ — **done in the refactor folder** (all four fns were unused); arrives in dev via the merge-back.
- ~~Watch: `regression.py` appeared to differ from the refactor copy during this work (possible earlier truncation of the dev copy around the EXPORT card, ~line 3924)~~ — **found + fixed (2026-07-04)**: `screen()`'s tail was truncated mid-statement; restored from HEAD and structurally verified (60/60 top-level defs match HEAD, no duplicate content). Still worth a real `python -m py_compile regression.py` + full click-through before the next release, since this session couldn't get a live compiler run against the connected folder (see UI polish note above).

---

## Gotchas (learned the hard way)
- **Never** copy/modify large files (`main.py`, `regression.py`, `engine.py`) via shell `cp`/`sed -i` on mounted folders — it can silently truncate. Use the Read/Edit/Write tools (host) or `_sync_to_install.py`.
- Flet 0.85 specifics: async window methods (`window.center()`/`destroy()` are coroutines), `HoverEvent.local_position` (Offset `.x`/`.y`, not `local_x`), `page.window.title_bar_hidden` for frameless.
- Class-level attributes (`HELP`, `PROVIDER_KEY_HELP`) sit between methods in `main.py` — mind them when moving code.
